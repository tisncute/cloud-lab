version: '3.8'

services:
  backend:
    build: ./mern-demo/server
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://leduonghuynhtinpt123_db_user:12092004@cluster0.rvcavaf.mongodb.net/cloud_lab?appName=Cluster0
      - PORT=5000

  frontend:
    build: ./client
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:5000
    depends_on:
      - backend